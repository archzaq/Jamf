#!/usr/bin/expect -f
set timeout 10
set password [lindex $argv 0]
set host [lindex $argv 1]

spawn ssh-copy-id -o StrictHostKeyChecking=accept-new USERNAME@$host
expect "Password:"
send "$password\r"
expect eof
